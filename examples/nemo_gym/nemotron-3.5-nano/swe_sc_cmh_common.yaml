# =============================================================================
# GRPO Ultra E2E — Production SWE Config for GB200 NVL72
# =============================================================================
# Production config for Nemotron 3.5 Nano SWE end-to-end GRPO training on
# 64 CMH nodes × 4 GPUs/node (32 generation + 32 training).
#
# Static algorithm, loss, and environment settings live here.
# Per-run overrides (model path, data paths, parallelism, precision)
# are applied by nano35_launch.sh or Hydra CLI.
#
#   - gpus_per_node: 4
#   - TP: 4, CP: 16, EP: 32, PP: 1
#   - vLLM TP: 4
#   - Non-colocated async inference with 32 generation nodes
#   - Max sequence length: 196608
# =============================================================================

# Inherit the exemplar so newly-required fields (e.g. policy.generation.val_*)
# are picked up automatically instead of being re-added here each time. Keys
# below override the base; base-only keys are inherited.
defaults: ../../configs/grpo_math_1B.yaml

# Overridden by nano35_launch.sh. The SWE container formatter below resolves all
# six instance-image families under this root.
sif_dir: null

# =============================================================================
# Cluster — overridden by launch script for parallelism changes
# =============================================================================
cluster:
  gpus_per_node: 4
  num_nodes: 64
  segment_size: 16

# =============================================================================
# Checkpointing
# =============================================================================
checkpointing:
  enabled: true
  checkpoint_dir: "results/grpo_ultra_e2e"
  # SingleController has no validation loop; keep checkpoints by recency/rung.
  metric_name: null
  higher_is_better: true
  keep_top_k: 1000000
  save_period: 5
  ft_keep_latest_k: 1
  ft_save_period: 1
  checkpoint_must_save_by: "02:23:00:00"
  model_save_format: "safetensors"
  save_consolidated: false

# =============================================================================
# GRPO Algorithm
# =============================================================================
grpo:
  # Aligned to the validated 32-node baseline run nvidia/ultra-v3-swe-e2e/xm4iyc6w
  # (sdd-swe-e2e-nano-...-pps32_gbs512-gen-nodes-32): PPS=32, GPP=16 -> GBS=512.
  num_prompts_per_step: 32
  num_generations_per_prompt: 16
  num_val_generations_per_prompt: 2
  max_rollout_turns: 1
  max_num_epochs: 4
  max_num_steps: 1000000
  normalize_rewards: true
  use_leave_one_out_baseline: true
  advantage_clip_low: -100
  advantage_clip_high: 100
  val_period: 0
  val_at_start: false
  val_at_end: false
  overlong_filtering: false
  max_val_samples: null
  val_batch_size: 256
  seed: 42

  use_dynamic_sampling: false
  dynamic_sampling_max_gen_batches: 10
  batch_multiplier: 1

  penalize_invalid_tool_call: true
  invalid_tool_call_advantage: -5.0
  penalize_malformed_thinking: true
  malformed_thinking_advantage: -5.0

  reward_shaping:
    enabled: false
    overlong_buffer_length: 128
    overlong_buffer_penalty: 1
    max_response_length: ${policy.max_total_sequence_length}
    stop_properly_penalty_coef: null
  reward_scaling:
    enabled: false
    source_min: 0.0
    source_max: 1.0
    target_min: 0.0
    target_max: 1.0

  # This recipe uses examples/run_grpo_single_controller.py.
  async_grpo: null

  use_best_at_k: false
  best_at_k_k: 8
  best_at_k_m: 1000

  use_combined_training: false
  combined_training_weight_mode: "auto"
  combined_training_best_at_k_weight: 0.2
  combined_training_pass_at_1_weight: 1.0

  dynamic_sampling_oversample_ratio: 1.0
  seq_logprob_error_threshold: 2
  skip_reference_policy_logprobs_calculation: true

# =============================================================================
# Loss Function
# =============================================================================
loss_fn:
  reference_policy_kl_penalty: 0.0
  reference_policy_kl_type: "k3"
  kl_input_clamp_value: null
  kl_output_clamp_value: null

  ratio_clip_min: 0.2
  ratio_clip_max: 0.28
  ratio_clip_c: null
  use_on_policy_kl_approximation: true
  use_importance_sampling_correction: true
  truncated_importance_sampling_ratio: 5
  truncated_importance_sampling_ratio_min: 0.2
  truncated_importance_sampling_type: tis
  sequence_level_importance_ratios: false
  token_level_loss: true
  force_on_policy_ratio: true
  use_kl_in_reward: false

# =============================================================================
# Policy
# =============================================================================
policy:
  model_name: "/lustre/fsw/portfolios/llmservice/users/wdai/megatron-lm-ultra/checkpoints/ultra-v3-sft-bf16-hybridep-ep64-cp32-bindpcie-recompute-offload-288k-nano-loss-032026/iter_0007000/hf"
  tokenizer:
    name: ${policy.model_name}
    chat_template_kwargs: null
  hf_config_overrides: {}

  train_global_batch_size: 512
  train_micro_batch_size: 1
  generation_batch_size: 64
  logprob_batch_size: 1
  max_total_sequence_length: 196608
  precision: "bfloat16"
  logprob_chunk_size: 1024
  offload_optimizer_for_logprob: false

  dtensor_cfg:
    _v2: true
    enabled: false
    cpu_offload: false
    sequence_parallel: false
    activation_checkpointing: false
    tensor_parallel_size: 1
    context_parallel_size: 1
    custom_parallel_plan: null

  megatron_cfg:
    enabled: true
    empty_unused_memory_level: 2
    activation_checkpointing: true

    tensor_model_parallel_size: 4
    expert_tensor_parallel_size: 1
    expert_model_parallel_size: 32
    pipeline_model_parallel_size: 1
    num_layers_in_first_pipeline_stage: null
    num_layers_in_last_pipeline_stage: null
    context_parallel_size: 16
    pipeline_dtype: ${policy.precision}
    sequence_parallel: true

    # MoE
    freeze_moe_router: true
    moe_router_dtype: "fp32"
    moe_router_load_balancing_type: "none"
    moe_router_bias_update_rate: 1.0e-3
    moe_router_enable_expert_bias: true
    moe_permute_fusion: true
    moe_enable_deepep: false
    moe_token_dispatcher_type: "alltoall"
    moe_flex_dispatcher_backend: "hybridep"
    moe_hybridep_num_sms: 32
    moe_aux_loss_coeff: 0.0
    moe_shared_expert_overlap: false
    use_gloo_process_groups: false

    # Compute
    apply_rope_fusion: true
    use_fused_weighted_squared_relu: true
    bias_activation_fusion: false
    # community_import.py reads megatron_cfg["gradient_accumulation_fusion"] directly
    # (no default) on the HF->megatron conversion path; absent here it raises KeyError.
    gradient_accumulation_fusion: false
    defer_fp32_logits: true

    # Logging
    track_moe_metrics: true
    moe_per_layer_logging: true
    do_not_average_loss: true
    cp_normalize: true
    calculate_per_token_loss: true
    scale_loss_by_dp_cp_size: false

    # MTP — disabled
    mtp_loss_scaling_factor: 0.0
    mtp_use_repeated_layer: true
    mtp_num_layers: 5
    mtp_detach_heads: true

    optimizer:
      optimizer: "adam"
      lr: 3.0e-6
      min_lr: 3.0e-6
      weight_decay: 0.0
      bf16: true
      fp16: false
      params_dtype: "float32"

      adam_beta1: 0.9
      adam_beta2: 0.999
      adam_eps: 1e-8

      sgd_momentum: 0.9

      use_distributed_optimizer: true
      use_precision_aware_optimizer: true

      clip_grad: ${policy.max_grad_norm}

      optimizer_cpu_offload: false
      optimizer_offload_fraction: 0.0

    scheduler:
      start_weight_decay: ${policy.megatron_cfg.optimizer.weight_decay}
      end_weight_decay: ${policy.megatron_cfg.optimizer.weight_decay}
      weight_decay_incr_style: "constant"
      lr_decay_style: "constant"
      lr_decay_iters: null
      lr_warmup_iters: 10
      lr_warmup_init: 3e-7
      override_opt_param_scheduler: true

    distributed_data_parallel_config:
      grad_reduce_in_fp32: false
      overlap_grad_reduce: false
      overlap_param_gather: true
      average_in_collective: false
      use_custom_fsdp: false
      data_parallel_sharding_strategy: "optim_grads_params"

    # FP8 — overridden by precision recipe in launch script
    fp8_cfg:
      enabled: false
      fp8: "e4m3"
      fp8_recipe: "mxfp8"
      fp8_param: false

    first_last_layers_bf16: true
    num_layers_at_start_in_bf16: 1
    num_layers_at_end_in_bf16: 1

    checkpoint:
      async_save: true
      ckpt_assume_constant_structure: true
      # NOTE: fully_parallel_{save,load}_process_group / fully_parallel_load_exchange_algo
      # were dropped from this checkout's megatron-bridge CheckpointConfig (submodule
      # 554c7b9); leaving them in raises InstantiationException "Unexpected config keys".
      # Removed for the current-repo reproduction.

    env_vars: null

  # ---------------------------------------------------------------------------
  # Sequence Packing
  # ---------------------------------------------------------------------------
  dynamic_batching:
    enabled: false
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    sequence_length_round: 64

  sequence_packing:
    enabled: true
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    algorithm: "modified_first_fit_decreasing"
    sequence_length_round: 64
    fuse_loss: true

  # Must be a multiple of minimum_pad_factor = cp_size*2*tp_size (see
  # nemo_rl/models/megatron/data.py). With CP=16 + TP=4 + sequence_parallel that is
  # 128; the old value (=TP=4) made get_logprobs assert and abort every training
  # step. Formula matches examples/nemo_gym/grpo_nanov3.yaml (TP*CP*2).
  make_sequence_length_divisible_by: ${mul:${mul:${policy.megatron_cfg.tensor_model_parallel_size}, ${policy.megatron_cfg.context_parallel_size}}, 2}
  max_grad_norm: 1.0
  optimizer: null
  scheduler: null

  # ---------------------------------------------------------------------------
  # Generation (vLLM) — Non-colocated, async
  # ---------------------------------------------------------------------------
  generation:
    port_range_low: 11001
    port_range_high: 15000
    backend: "vllm"
    refit_transport: nccl_reshard
    max_new_tokens: ${policy.max_total_sequence_length}
    temperature: 1.0
    top_p: 1.0
    top_k: null
    stop_token_ids: null
    stop_strings: null
    vllm_cfg:
      async_engine: true
      precision: ${policy.precision}
      kv_cache_dtype: "auto"
      tensor_parallel_size: 4
      pipeline_parallel_size: 1
      expert_parallel_size: 1
      gpu_memory_utilization: 0.85
      max_model_len: ${policy.max_total_sequence_length}
      enforce_eager: false
      use_deep_gemm: false
      num_last_layers_in_bf16: 0
      num_first_layers_in_bf16: 0
      enable_vllm_metrics_logger: true
      vllm_metrics_logger_interval: 0.5
      expose_http_server: true
      skip_tokenizer_init: false
      # reasoning_parser_plugin lives at vllm_cfg top level in this checkout
      # (vllm_worker_async reads vllm_cfg["reasoning_parser_plugin"]); passing it
      # inside http_server_serving_chat_kwargs makes OpenAIServingChat raise
      # "unexpected keyword argument 'reasoning_parser_plugin'". Path updated to this
      # repo's location (the parser moved out of nemo_rl/utils/).
      reasoning_parser_plugin: nemo_rl/models/generation/vllm/reasoning_parsers/nano_v3_reasoning_parser.py
      http_server_serving_chat_kwargs:
        enable_auto_tools: true
        tool_parser: qwen3_coder
        reasoning_parser: nano_v3
        # Match the validated stage2_swe2 baseline: render the thinking structure
        # so multi-turn <think> content is handled consistently.
        default_chat_template_kwargs:
          enable_thinking: true

    vllm_kwargs:
      attention_backend: FLASH_ATTN
      moe_backend: triton
      max_num_batched_tokens: 8480
      mamba_ssm_cache_dtype: "float32"
      mamba_cache_mode: align
      compilation_config:
        cudagraph_capture_sizes: [1,2,4,8,16,32,64]
        pass_config:
          fuse_allreduce_rms: false

    colocated:
      enabled: false
      resources:
        gpus_per_node: 4
        num_nodes: 32

# =============================================================================
# Data
# =============================================================================
data:
  max_input_seq_length: null
  shuffle: false
  num_workers: 1
  train:
    data_path: null   # Set by launch script
    # The inherited exemplar (defaults: ../../configs/grpo_math_1B.yaml) injects
    # data.train.{dataset_name: OpenMathInstruct-2, split_validation_size, seed}.
    # update_single_dataset_config() only fills MISSING keys, so data.default's
    # dataset_name below would NOT override them -> the SWE blend would load as
    # OpenMathInstruct-2 and crash nemo_gym_data_processor with
    # KeyError: 'extra_env_info'. Pin them here so the split loads via NemoGymDataset.
    dataset_name: NemoGymDataset
    split_validation_size: null
    seed: null
  validation:
    data_path: null   # Set by launch script
    dataset_name: NemoGymDataset
  default:
    dataset_name: NemoGymDataset
    env_name: "nemo_gym"
    prompt_file: null
    system_prompt_file: null
    processor: "nemo_gym_data_processor"

# =============================================================================
# Environment — NeMo Gym + SWE Agents
# =============================================================================
env:
  should_use_nemo_gym: true
  should_log_nemo_gym_responses: true
  nemo_gym:
    # nano35_launch.sh overrides this per submission for response artifacts.
    nemo_gym_log_dir: null
    # Reuse pre-built gym venvs. With false, all 3 gym servers (policy_model,
    # swe_agents_{train,val}) rebuild the editable nemo-gym package in parallel and
    # contend on the same uv distribution-cache lock on Lustre; policy_model then
    # times out (default 300s) and NemoGym spinup fails. Mirrors the qwen-30b SWE run.
    skip_venv_if_present: true
    num_gpu_nodes: 0
    port_range_low: 15001
    port_range_high: 20000
    invalid_tool_call_patterns:
      - "<tool_call>"
      - "</tool_call>"
      - "<function_call>"
      - "</function_call>"
    thinking_tags:
      - "<think>"
      - "</think>"
    config_paths:
    - responses_api_models/vllm_model/configs/vllm_model_for_training.yaml
    - responses_api_agents/swe_agents/configs/swebench_openhands_training.yaml
    # Override the gym policy_model to match the validated stage2_swe2 baseline.
    # truncate_history_thinking: false is critical -- keeping prior-turn <think>
    # content in the re-rendered multi-turn history preserves the monotonic-token
    # property, so generation logprobs align with training recompute. Without it
    # (gym default truncates thinking) the re-templated prefix is shorter than the
    # original generated tokens -> _replace_prefix_tokens non-monotonic assertion
    # -> generation/training logprob misalignment -> mass seq_logprob_error masking.
    policy_model:
      responses_api_models:
        vllm_model:
          num_workers: 16
          chat_template_kwargs:
            enable_thinking: true
            truncate_history_thinking: false
          sequential_reasoning_allowed: false
    policy_model_reasoning_off:
      responses_api_models:
        vllm_model:
          num_workers: 4
    swe_agents_train:
      responses_api_agents:
        swe_agents:
          agent_max_turns: 200
          concurrency: ${mul:2, ${mul:${grpo.num_prompts_per_step}, ${grpo.num_generations_per_prompt}}}
          swebench_agent_timeout: 3600
          run_with_mixed_prompts: true
          dataset_path: ${data.train.data_path}
          apptainer_memory_limit_mb: 65536
          container_formatter:
            - "${sif_dir}/swerebench/{instance_id}.sif"
            - "${sif_dir}/nv_internal/{instance_id}.sif"
            - "${sif_dir}/r2e_gym/{instance_id}.sif"
            - "${sif_dir}/swegym/sweb.eval.arm64.{instance_id}.sif"
            - "${sif_dir}/swebench/swe-bench.eval.arm64.{instance_id}.sif"
            - "${sif_dir}/mercor/swebenchpro_ots/{instance_id}.sif"
    swe_agents_val:
      responses_api_agents:
        swe_agents:
          agent_max_turns: 200
          concurrency: ${mul:2, ${mul:${grpo.num_prompts_per_step}, ${grpo.num_generations_per_prompt}}}
          swebench_agent_timeout: 3600
          dataset_path: ${data.validation.data_path}
          apptainer_memory_limit_mb: 65536
          container_formatter: ${env.nemo_gym.swe_agents_train.responses_api_agents.swe_agents.container_formatter}
    use_absolute_ip: true

# =============================================================================
# Logger
# =============================================================================
logger:
  log_dir: "logs"
  num_val_samples_to_print: 0
  wandb_enabled: true
  tensorboard_enabled: false
  mlflow_enabled: false
  monitor_gpus: true
  swanlab_enabled: false
  wandb:
    project: "ultra-v3-swe-e2e-convergence"
    name: "nano35-swe-v2"
  tensorboard: {}
  mlflow:
    experiment_name: "grpo-ultra-e2e"
    run_name: "grpo-ultra-e2e"
  gpu_monitoring:
    collection_interval: 10
    flush_interval: 10

# =============================================================================
# Effort Levels
# =============================================================================
effort_levels:
  low_string: "{reasoning effort: efficient}"
  low_weight: 0.1
  low_penalty: 1
  low_ub: 15000

# =============================================================================
# Reward Penalties (set reward to 0 when triggered)
# MasterConfig.reward_penalties -> RewardPenaltyConfig (nemo_rl/algorithms/grpo.py).
# MUST be nested under `reward_penalties:`; a top-level `penalize_*`/`token_ids`
# is silently accepted (MasterConfig extra="allow") but ignored, so the penalties
# never fire. `token_ids.unwanted` is a list[int] and is required when
# penalize_unwanted_tokens is true.
# =============================================================================
reward_penalties:
  penalize_duplicated_reasoning: true   # reasoning content == final answer
  penalize_empty_final_answer: true     # last message output has empty content
  penalize_unwanted_tokens: true        # an unwanted token id appears in generation
  penalize_malformed_think_tag: true    # <think>/</think> count != 1 per turn
  token_ids:
    unwanted: [2]     # </s> eos
    think_open: 12    # <think>
    think_close: 13   # </think>

# =============================================================================
# SingleController async RL
# =============================================================================
async_rl:
  sampler:
    name: in_order
    max_lookahead_versions: 1
  recompute_kv_cache_after_weight_updates: false
  # The arm overlays set this to 32 (no streaming) or 4 (streaming-4).
  min_groups_for_streaming_train: 32
  # One live 32-group cohort plus one lookahead cohort.
  max_inflight_prompts: 64
  max_buffered_rollouts: 64
  rollout_failure:
    max_infra_attempts_per_prompt: 5
    max_data_attempts_per_prompt: 2
    backoff_base_s: 1.0
    max_backoff_s: 30.0
    max_consecutive_dropped_prompts: 32
    max_skipped_prompts: 32
    on_dropped_prompt: replace
    max_replacement_attempts: 1
    replacement_reserve_prompts: 1
    min_step_batch_fraction: 0.9
    nemo_gym:
      rollout_timeout_s: 7200.0
  stall_watchdog:
    interval_s: 30.0
    stall_timeout_s: 7500.0
    stall_action: warn
    gym_subprocess_check: true

# =============================================================================
# TransferQueue data plane
# =============================================================================
data_plane:
  enabled: true
  impl: transfer_queue
  backend: "simple"
  storage_capacity: 1000000
  num_storage_units: 16
  claim_meta_poll_interval_s: 0.5
  global_segment_size: 549755813888
  local_buffer_size: 68719476736
